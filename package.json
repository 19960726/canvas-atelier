{
  "name": "canvas-agent-mvp",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "test": "vitest run --config vitest.config.ts",
    "typecheck": "tsc -p packages/domain/tsconfig.json --noEmit && tsc -p packages/skill-store/tsconfig.json --noEmit && tsc -p packages/desktop-core/tsconfig.json --noEmit && tsc -p apps/desktop-legacy/tsconfig.json --noEmit && tsc -p apps/desktop-modern/tsconfig.json --noEmit && tsc -p apps/renderer/tsconfig.json --noEmit",
    "build": "npm run typecheck && npm run build -w @agent-canvas/desktop-core && npm run build -w @agent-canvas/renderer && npm run build -w @agent-canvas/desktop-legacy && npm run build -w @agent-canvas/desktop-modern",
    "e2e": "echo \"e2e not configured yet\""
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "esbuild": "0.25.6",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
