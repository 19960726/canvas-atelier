# Observed Canvas Capability Compatibility Matrix

## Scope and provenance

This matrix maps only publicly visible categories observed in user-provided screenshots and ordinary observable behavior to original Novus Atelier concepts. It is a planning reference, not a reverse-engineering record. CanvasForge is observation-only and must not be unpacked, decompiled, copied, credential-inspected, source-inspected, or used to reproduce branding, UI, code, or trade dress.

Music and speech capabilities are inventoried for future research but disabled in this delivery. They expose no executable Novus module, provider route, or user-facing action.

| Visible category | Observed purpose | Novus Atelier module | Typed inputs → outputs | Execution mode | Intended use | Safety boundary |
| --- | --- | --- | --- | --- | --- | --- |
| Image | Bring a source visual into a workflow. | 图像输入 / Image Input | `image-asset-ref` → `image-asset-ref`, `image-metadata` | Local managed-asset ingestion | Start an image-generation, editing, comparison, or analysis chain. | Accept managed opaque asset IDs only; validate type/size; no Base64 originals, private path exposure, or arbitrary renderer file access. |
| Text | Supply a written instruction or constraint. | 文本提示 / Text Prompt | `text-prompt`, `negative-constraints` → `prompt-spec` | Local schema validation | Define generation intent, constraints, and reverse-analysis materialization. | Sanitize summaries; do not persist provider credentials, authorization headers, or raw provider payloads. |
| Storyboard sheet/chart | Arrange visual beats or shots in sequence. | 分镜表 / Storyboard Sheet | `shot-timeline`, `prompt-spec[]`, `image-asset-ref[]` → `storyboard-sheet`, `storyboard-chart` | Local composition; optional explicit materialization | Plan visual sequencing and hand off selected shots to generation. | No automatic generation; materialization requires confirmation and retains only opaque asset references. |
| Painting/mask | Mark a region for selective work. | 绘制蒙版 / Drawing Mask | `image-asset-ref`, `mask-strokes` → `mask-asset-ref`, `edit-region` | Local canvas editing | Define an edit or redraw region. | Store stable mask assets only; transient pointer/stroke previews are not persisted; renderer lacks direct filesystem access. |
| Video | Use a clip as a source for shot-level understanding. | 视频解析 / Video Reverse Analysis | `video-asset-ref`, `time-range`, `analysis-template` → `shot-timeline`, `analysis-report`, `citations` | Controlled long-video provider job | Break a clip into recoverable, reviewable visual analysis. | Route only to long-video-capable profiles; bind revision/nonce; use segment retry/cancel; never expose raw task IDs or arbitrary URLs. |
| Agent | Assist with workflow planning and interpretation. | 工作流助手 / Workflow Agent | `project-context`, `prompt-spec`, `analysis-report` → `workflow-suggestion`, `explanation` | Local orchestration plus capability-routed provider call | Suggest connections, settings, and explanations in a collapsible dock. | Dock reflows canvas; no automatic destructive writes; provider routing is confirmed and frozen; renderer receives no tokens or raw provider data. |
| Comfy UI | Execute a constrained visual workflow description. | 受控工作流 / Controlled Comfy Workflow | `sanitized-comfy-workflow`, `image-asset-ref[]`, `prompt-spec` → `generation-job`, `image-asset-ref[]` | Allowlisted provider adapter | Run an explicitly imported compatible workflow. | Reject credentials, absolute paths, scripts, protected payloads, and nodes outside the allowlist; renderer has no provider credentials or arbitrary network/filesystem access. |
| Compare | Inspect alternatives side by side. | 图像对比 / Image Compare | `image-asset-ref[2+]`, `comparison-selection` → `comparison-view`, `selection` | Local view composition | Select a preferred result or compare history/project copies. | Uses opaque managed references; comparison does not duplicate originals, disclose private paths, or alter history without explicit action. |
| Local redraw | Modify a masked image locally within a workflow. | 局部重绘 / Local Redraw | `image-asset-ref`, `mask-asset-ref`, `prompt-spec` → `generation-job`, `image-asset-ref` | Capability-routed generation/edit job | Request a targeted image revision from a confirmed region and prompt. | Requires a valid managed image/mask and explicit confirmed provider route; redacts provider data and treats results as managed assets. |

## Compatibility principles

The matrix describes capability parity at the category level only. It does not assert identical behavior, interface, workflow syntax, or implementation. Novus modules retain original Chinese-primary/English-secondary names, typed contracts, execution states, real Lucide icons, and explicit errors.

Provider calls cross the desktop boundary through narrow typed bridges. Provider selection uses declared capabilities and user confirmation. Project persistence records stable transactions and durable acknowledgement only; pan, zoom, pointer movement, connection preview, sort preview, and transient drag state stay outside durable data. All generated and imported media are managed assets referenced by opaque IDs.
